apiVersion: v1
kind: PersistentVolume
metadata:
  name: hub-db-dir-nfs
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-shared
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 172.16.2.40
    path: /mnt/shared-data/hub-db-dir
