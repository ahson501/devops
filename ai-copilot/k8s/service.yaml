apiVersion: v1
kind: Service
metadata:
  name: ai-copilot
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: ai-copilot
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007
